# Staged non-deceptive payloads for E2E verification only (not live market claims).
# IMPORTANT: `gcloud run services update --env-vars-file` replaces the entire env block.
# Always include PROJECT_ID in this file (or run a separate `--update-env-vars=PROJECT_ID=...` after).
# Used with: gcloud run services update genie-blog-run --region=asia-northeast3 --env-vars-file=ops/staged-today-feeds.env.yaml
PROJECT_ID: gen-lang-client-0667098249
TODAY_GENIE_OVERNIGHT_US_MARKET_JSON: '{"staged_verification":true,"label":"e2e_check","summary":{"indices_note":"placeholder numbers for pipeline test only"}}'
TODAY_GENIE_MACRO_INDICATORS_JSON: '{"staged_verification":true,"label":"e2e_check","rates":{"note":"placeholder"}}'
TODAY_GENIE_TOP_MARKET_NEWS_JSON: '[{"staged_verification":true,"headline":"E2E verification headline","source":"staged"}]'
TODAY_GENIE_RISK_FACTORS_JSON: '[{"staged_verification":true,"risk":"E2E verification","detail":"staged risk line"}]'
