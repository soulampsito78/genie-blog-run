# Customer backend (customer/) dependencies.
#
# Kept as a separate lock surface so customer/database changes stay auditable.
# The co-located API and worker images install this file explicitly because the
# delegated delivery adapter imports the PostgreSQL customer authority at the
# final eligibility boundary.  Keeping separate files must not mean omitting
# required runtime dependencies from those images.
#
# Install:  pip install -r requirements-customer.txt

SQLAlchemy>=2.0,<3.0
alembic>=1.13
psycopg[binary]>=3.1
